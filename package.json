{
    "name": "code-sanctuary-root",
    "type": "module",
    "private": true,
    "workspaces": [
        "client",
        "server"
    ],
    "scripts": {
        "dev:client": "npm run dev -w code-sanctuary-client",
        "dev:server": "npm run dev -w code-sanctuary-server",
        "install:all": "npm install && npm install -w code-sanctuary-client && npm install -w code-sanctuary-server"
    },
    "dependencies": {
        "zod": "^3.24.2"
    }
}